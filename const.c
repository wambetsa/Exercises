#include <stdio.h>
/*constants in c*/
int main()
{
    const int length = 33;
    const int width = 20;
    float area = (length * width);

    printf("Area of Rectange = %f\n", area);
    return 0;
}